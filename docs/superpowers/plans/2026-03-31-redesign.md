# HealthVault Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete CSS redesign with warm, independent visual identity and adaptive navigation (three different nav patterns per screen size).

**Architecture:** Pure CSS rewrite using CSS custom properties. Layout.tsx rewritten for adaptive navigation (desktop sidebar, tablet top-bar, mobile fullscreen overlay). All 227+ CSS class names preserved so page components need zero changes. UI store extended for sidebar collapsed state and active nav group.

**Tech Stack:** React 19, TypeScript, CSS Custom Properties, Zustand

**Design Spec:** `docs/superpowers/specs/2026-03-31-redesign-design.md`

---

## File Structure

### Modified Files
- `web/src/index.css` — Complete rewrite: design tokens only (colors, spacing, typography, animations, z-index)
- `web/src/App.css` — Complete rewrite: all styles (~3,356 lines current → ~1,800 lines new)
- `web/src/components/Layout.tsx` — Complete rewrite: adaptive navigation (desktop sidebar + tablet top-bar + mobile overlay)
- `web/src/store/ui.ts` — Extended: add `sidebarCollapsed` (persisted), `activeNavGroup`
- `web/src/i18n/en.json` — Add missing nav keys: `nav.group_system`, `nav.emergency`, `nav.activity`
- `web/src/i18n/de.json` — Same keys in German

### Unchanged Files (zero modifications needed)
- All 28 page components in `web/src/pages/` — CSS class names preserved
- `web/src/App.tsx` — Router structure unchanged
- `web/src/api/*` — API layer unchanged
- `web/src/store/auth.ts` — Auth store unchanged
- `web/src/hooks/*` — All hooks unchanged
- `web/src/crypto/*` — Crypto module unchanged
- `web/src/components/NotificationBell.tsx` — CSS class names preserved
- `web/src/components/SyncIndicator.tsx` — CSS class names preserved
- `web/src/components/ProfileSelector.tsx` — CSS class names preserved
- `web/src/components/ConfirmDelete.tsx` — CSS class names preserved
- `web/src/components/TrendPanel.tsx` — CSS class names preserved
- `web/src/components/OCRUpload.tsx` — CSS class names preserved
- `web/src/components/ErrorBoundary.tsx` — CSS class names preserved

---

### Task 1: CSS Design Tokens

**Files:**
- Modify: `web/src/index.css`

- [ ] **Step 1: Rewrite index.css with new design tokens**

Replace the entire file with:

```css
/* HealthVault Design Tokens */
:root {
  /* Backgrounds — warm sand/beige undertone, never pure gray */
  --bg: #FAF8F5;
  --bg-subtle: #F3F0EB;
  --surface: #FFFFFF;
  --surface-raised: #FFFFFF;
  --surface-hover: #F9F7F4;

  /* Text — warm near-black, never pure #000 */
  --text: #2C2825;
  --text-secondary: #7A7269;
  --text-tertiary: #A8A099;

  /* Borders */
  --border: #E8E3DC;
  --border-subtle: #F0ECE6;

  /* Shadows — warm-tinted */
  --shadow-sm: 0 1px 3px rgba(44, 40, 37, 0.06);
  --shadow: 0 2px 6px rgba(44, 40, 37, 0.07);
  --shadow-md: 0 4px 12px rgba(44, 40, 37, 0.08);
  --shadow-lg: 0 8px 24px rgba(44, 40, 37, 0.10);
  --shadow-xl: 0 12px 32px rgba(44, 40, 37, 0.12);
  --shadow-card-hover: 0 4px 16px rgba(44, 40, 37, 0.10);

  /* Primary — Terracotta/Copper */
  --primary: #B8704A;
  --primary-hover: #A5623F;
  --primary-light: rgba(184, 112, 74, 0.08);
  --primary-glow: rgba(184, 112, 74, 0.15);
  --primary-text: #FFFFFF;

  /* Semantic */
  --success: #5A8A5C;
  --success-light: rgba(90, 138, 92, 0.10);
  --warning: #C4933B;
  --warning-light: rgba(196, 147, 59, 0.10);
  --danger: #C4513B;
  --danger-light: rgba(196, 81, 59, 0.10);
  --critical: #A3392A;
  --info: #5A7A8A;
  --info-light: rgba(90, 122, 138, 0.10);

  /* Sizing */
  --sidebar-w: 240px;
  --sidebar-collapsed: 64px;
  --sidebar-width: var(--sidebar-w);
  --sidebar-collapsed-width: var(--sidebar-collapsed);

  /* Border Radius */
  --radius-sm: 6px;
  --radius: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;
  --radius-full: 9999px;

  /* Typography */
  --font: system-ui, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
  --font-sans: var(--font);
  --font-display: var(--font);
  --font-mono: 'Cascadia Code', 'Fira Code', 'SF Mono', ui-monospace, monospace;

  /* Spacing (8px grid) */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;

  /* Animation */
  --duration-fast: 150ms;
  --duration-base: 200ms;
  --duration-smooth: 250ms;
  --duration-slow: 350ms;
  --ease: cubic-bezier(0.4, 0, 0.2, 1);
  --transition-fast: var(--duration-fast) var(--ease);
  --transition: var(--duration-base) var(--ease);
  --transition-slow: var(--duration-slow) var(--ease);

  /* Z-index layers */
  --z-sidebar: 100;
  --z-dropdown: 150;
  --z-overlay: 200;
  --z-modal: 300;
  --z-toast: 400;

  /* Aliases for backward compat with components */
  --color-bg: var(--bg);
  --color-bg-subtle: var(--bg-subtle);
  --color-surface: var(--surface);
  --color-surface-raised: var(--surface-raised);
  --color-text: var(--text);
  --color-text-secondary: var(--text-secondary);
  --color-text-tertiary: var(--text-tertiary);
  --color-border: var(--border);
  --color-border-subtle: var(--border-subtle);
  --color-primary: var(--primary);
  --color-primary-hover: var(--primary-hover);
  --color-primary-light: var(--primary-light);
  --color-primary-glow: var(--primary-glow);
  --color-success: var(--success);
  --color-success-light: var(--success-light);
  --color-warning: var(--warning);
  --color-warning-light: var(--warning-light);
  --color-danger: var(--danger);
  --color-danger-light: var(--danger-light);
  --color-critical: var(--critical);
  --color-info: var(--info);
  --color-info-light: var(--info-light);
  --color-sidebar-bg: var(--surface);
  --color-sidebar-border: var(--border);
}

/* Dark Theme — warm brown undertone, never cold gray */
[data-theme="dark"] {
  --bg: #1C1A17;
  --bg-subtle: #242119;
  --surface: #2A2621;
  --surface-raised: #332E28;
  --surface-hover: #332E28;

  --text: #EDE9E3;
  --text-secondary: #9C958C;
  --text-tertiary: #6B645C;

  --border: #3A352E;
  --border-subtle: #302B25;

  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.20);
  --shadow: 0 2px 6px rgba(0, 0, 0, 0.24);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.28);
  --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.35);
  --shadow-xl: 0 12px 32px rgba(0, 0, 0, 0.40);
  --shadow-card-hover: 0 4px 16px rgba(0, 0, 0, 0.30);

  --primary: #D4956E;
  --primary-hover: #C4845D;
  --primary-light: rgba(212, 149, 110, 0.12);
  --primary-glow: rgba(212, 149, 110, 0.20);

  --success: #7AAE7C;
  --success-light: rgba(122, 174, 124, 0.15);
  --warning: #D4A84E;
  --warning-light: rgba(212, 168, 78, 0.15);
  --danger: #D4715B;
  --danger-light: rgba(212, 113, 91, 0.15);
  --info: #7A9AAA;
  --info-light: rgba(122, 154, 170, 0.15);
}
```

- [ ] **Step 2: Verify build**

Run: `cd web && npx tsc --noEmit`
Expected: No type errors (CSS-only change)

- [ ] **Step 3: Commit**

```bash
git add web/src/index.css
git commit -m "style: replace design tokens with warm terracotta palette"
```

---

### Task 2: UI Store Extension

**Files:**
- Modify: `web/src/store/ui.ts`

- [ ] **Step 1: Extend UI store with sidebarCollapsed and activeNavGroup**

Replace the entire file with:

```typescript
import { create } from 'zustand';

interface UIState {
  sidebarOpen: boolean;
  sidebarCollapsed: boolean;
  activeNavGroup: string;
  theme: 'light' | 'dark';
  toggleSidebar: () => void;
  toggleSidebarCollapsed: () => void;
  setActiveNavGroup: (group: string) => void;
  toggleTheme: () => void;
  setTheme: (theme: 'light' | 'dark') => void;
}

export const useUIStore = create<UIState>((set) => ({
  sidebarOpen: true,
  sidebarCollapsed: localStorage.getItem('sidebar_collapsed') === 'true',
  activeNavGroup: 'health',
  theme: (localStorage.getItem('theme') as 'light' | 'dark') || 'light',

  toggleSidebar: () => set((s) => ({ sidebarOpen: !s.sidebarOpen })),

  toggleSidebarCollapsed: () =>
    set((s) => {
      const next = !s.sidebarCollapsed;
      localStorage.setItem('sidebar_collapsed', String(next));
      return { sidebarCollapsed: next };
    }),

  setActiveNavGroup: (group) => set({ activeNavGroup: group }),

  toggleTheme: () =>
    set((s) => {
      const next = s.theme === 'light' ? 'dark' : 'light';
      localStorage.setItem('theme', next);
      document.documentElement.setAttribute('data-theme', next);
      return { theme: next };
    }),

  setTheme: (theme) => {
    localStorage.setItem('theme', theme);
    document.documentElement.setAttribute('data-theme', theme);
    set({ theme });
  },
}));
```

- [ ] **Step 2: Verify type-check**

Run: `cd web && npx tsc --noEmit`
Expected: No type errors

- [ ] **Step 3: Commit**

```bash
git add web/src/store/ui.ts
git commit -m "feat: extend UI store with sidebar collapsed state and nav group"
```

---

### Task 3: i18n Updates

**Files:**
- Modify: `web/src/i18n/en.json`
- Modify: `web/src/i18n/de.json`

- [ ] **Step 1: Add missing nav keys to en.json**

Open `web/src/i18n/en.json` and add these keys to the nav section (if not already present):

```json
"nav.group_system": "System",
"nav.emergency": "Emergency",
"nav.activity": "Activity"
```

- [ ] **Step 2: Add missing nav keys to de.json**

Open `web/src/i18n/de.json` and add:

```json
"nav.group_system": "System",
"nav.emergency": "Notfall",
"nav.activity": "Aktivität"
```

- [ ] **Step 3: Commit**

```bash
git add web/src/i18n/en.json web/src/i18n/de.json
git commit -m "i18n: add missing nav keys for system group"
```

---

### Task 4: Layout Component Rewrite

**Files:**
- Modify: `web/src/components/Layout.tsx`

- [ ] **Step 1: Rewrite Layout.tsx with adaptive navigation**

Replace the entire file. The new component renders three navigation variants — CSS controls which is visible per breakpoint:

- **Desktop (≥1024px):** Compact collapsible sidebar (left, full height, 240px/64px)
- **Tablet (640–1023px):** Sticky top-bar with group tabs + scrollable sub-page pills
- **Mobile (<640px):** Slim header + fullscreen overlay on hamburger tap

Key structural changes from current:
- Navigation items organized into 4 groups (health, tracking, manage, system) instead of flat lists
- Three separate JSX blocks for desktop/tablet/mobile (CSS `display: none` toggles visibility)
- `activeNavGroup` state auto-derived from current route, used for tablet sub-nav
- `mobileMenuOpen` as local state for overlay toggle
- `sidebarCollapsed` from store (persisted) for desktop sidebar

All SVG icons from the current file are preserved unchanged.

```typescript
import { useState, useEffect, useRef, useMemo, type ReactNode } from 'react';
import { Link, Outlet, useNavigate, useLocation } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { useAuthStore } from '../store/auth';
import { useUIStore } from '../store/ui';
import { api } from '../api/client';
import { clearAllKeys } from '../crypto';
import { NotificationBell } from './NotificationBell';
import { SyncIndicator } from './SyncIndicator';
import { useIdleTimeout } from '../hooks/useIdleTimeout';

interface NavItem {
  path: string;
  label: string;
  icon: ReactNode;
}

interface NavGroup {
  key: string;
  label: string;
  icon: ReactNode;
  items: NavItem[];
}

const svgProps = {
  width: 24,
  height: 24,
  viewBox: '0 0 24 24',
  fill: 'none',
  stroke: 'currentColor',
  strokeWidth: 1.5,
  strokeLinecap: 'round' as const,
  strokeLinejoin: 'round' as const,
};

const icons = {
  dashboard: (
    <svg {...svgProps}>
      <rect x="3" y="3" width="7" height="7" rx="1" />
      <rect x="14" y="3" width="7" height="7" rx="1" />
      <rect x="3" y="14" width="7" height="7" rx="1" />
      <rect x="14" y="14" width="7" height="7" rx="1" />
    </svg>
  ),
  search: (
    <svg {...svgProps}>
      <circle cx="11" cy="11" r="8" />
      <path d="M21 21l-4.35-4.35" />
    </svg>
  ),
  heart: (
    <svg {...svgProps}>
      <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" />
    </svg>
  ),
  beaker: (
    <svg {...svgProps}>
      <path d="M9 3h6" />
      <path d="M10 3v6.5L4 18h16l-6-8.5V3" />
    </svg>
  ),
  pill: (
    <svg {...svgProps}>
      <path d="M10.5 1.5l-8 8a4.95 4.95 0 0 0 7 7l8-8a4.95 4.95 0 0 0-7-7z" />
      <path d="M6.5 10.5l7-7" />
    </svg>
  ),
  syringe: (
    <svg {...svgProps}>
      <path d="M18 2l4 4" />
      <path d="M17 7l-10 10" />
      <path d="M19 5l-1.5 1.5" />
      <path d="M7 17l-4 4" />
      <path d="M15 5l-8.5 8.5" />
      <path d="M9 11l4 4" />
    </svg>
  ),
  alertTriangle: (
    <svg {...svgProps}>
      <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
      <line x1="12" y1="9" x2="12" y2="13" />
      <line x1="12" y1="17" x2="12.01" y2="17" />
    </svg>
  ),
  clipboardList: (
    <svg {...svgProps}>
      <path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2" />
      <rect x="8" y="2" width="8" height="4" rx="1" />
      <path d="M9 12h6" />
      <path d="M9 16h6" />
    </svg>
  ),
  bookOpen: (
    <svg {...svgProps}>
      <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z" />
      <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z" />
    </svg>
  ),
  activity: (
    <svg {...svgProps}>
      <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
    </svg>
  ),
  calendar: (
    <svg {...svgProps}>
      <rect x="3" y="4" width="18" height="18" rx="2" />
      <line x1="16" y1="2" x2="16" y2="6" />
      <line x1="8" y1="2" x2="8" y2="6" />
      <line x1="3" y1="10" x2="21" y2="10" />
    </svg>
  ),
  checkSquare: (
    <svg {...svgProps}>
      <polyline points="9 11 12 14 22 4" />
      <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11" />
    </svg>
  ),
  fileText: (
    <svg {...svgProps}>
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
      <polyline points="14 2 14 8 20 8" />
      <line x1="16" y1="13" x2="8" y2="13" />
      <line x1="16" y1="17" x2="8" y2="17" />
      <polyline points="10 9 9 9 8 9" />
    </svg>
  ),
  users: (
    <svg {...svgProps}>
      <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
      <circle cx="9" cy="7" r="4" />
      <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
      <path d="M16 3.13a4 4 0 0 1 0 7.75" />
    </svg>
  ),
  rss: (
    <svg {...svgProps}>
      <path d="M4 11a9 9 0 0 1 9 9" />
      <path d="M4 4a16 16 0 0 1 16 16" />
      <circle cx="5" cy="19" r="1" />
    </svg>
  ),
  share: (
    <svg {...svgProps}>
      <circle cx="18" cy="5" r="3" />
      <circle cx="6" cy="12" r="3" />
      <circle cx="18" cy="19" r="3" />
      <line x1="8.59" y1="13.51" x2="15.42" y2="17.49" />
      <line x1="15.41" y1="6.51" x2="8.59" y2="10.49" />
    </svg>
  ),
  shield: (
    <svg {...svgProps}>
      <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    </svg>
  ),
  settings: (
    <svg {...svgProps}>
      <circle cx="12" cy="12" r="3" />
      <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09a1.65 1.65 0 0 0-1.08-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09a1.65 1.65 0 0 0 1.51-1.08 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1.08z" />
    </svg>
  ),
  moon: (
    <svg {...svgProps}>
      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
    </svg>
  ),
  sun: (
    <svg {...svgProps}>
      <circle cx="12" cy="12" r="5" />
      <line x1="12" y1="1" x2="12" y2="3" />
      <line x1="12" y1="21" x2="12" y2="23" />
      <line x1="4.22" y1="4.22" x2="5.64" y2="5.64" />
      <line x1="18.36" y1="18.36" x2="19.78" y2="19.78" />
      <line x1="1" y1="12" x2="3" y2="12" />
      <line x1="21" y1="12" x2="23" y2="12" />
      <line x1="4.22" y1="19.78" x2="5.64" y2="18.36" />
      <line x1="18.36" y1="5.64" x2="19.78" y2="4.22" />
    </svg>
  ),
  logOut: (
    <svg {...svgProps}>
      <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
      <polyline points="16 17 21 12 16 7" />
      <line x1="21" y1="12" x2="9" y2="12" />
    </svg>
  ),
  download: (
    <svg {...svgProps}>
      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
      <polyline points="7 10 12 15 17 10" />
      <line x1="12" y1="15" x2="12" y2="3" />
    </svg>
  ),
  emergency: (
    <svg {...svgProps}>
      <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" />
      <line x1="12" y1="9" x2="12" y2="15" />
      <line x1="9" y1="12" x2="15" y2="12" />
    </svg>
  ),
  menu: (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
      <path d="M3 12h18M3 6h18M3 18h18" />
    </svg>
  ),
  close: (
    <svg {...svgProps}>
      <line x1="18" y1="6" x2="6" y2="18" />
      <line x1="6" y1="6" x2="18" y2="18" />
    </svg>
  ),
  chevronDown: (
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <polyline points="6 9 12 15 18 9" />
    </svg>
  ),
  collapse: (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
      <polyline points="11 17 6 12 11 7" />
      <polyline points="18 17 13 12 18 7" />
    </svg>
  ),
  expand: (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
      <polyline points="13 17 18 12 13 7" />
      <polyline points="6 17 11 12 6 7" />
    </svg>
  ),
};

export function Layout() {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const location = useLocation();
  const { logout, role, email } = useAuthStore();
  const {
    sidebarCollapsed, toggleSidebarCollapsed,
    sidebarOpen, toggleSidebar,
    activeNavGroup, setActiveNavGroup,
    theme, toggleTheme,
  } = useUIStore();
  useIdleTimeout();

  const [avatarMenuOpen, setAvatarMenuOpen] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [avatarUrl, setAvatarUrl] = useState<string | null>(localStorage.getItem('user_avatar'));
  const avatarMenuRef = useRef<HTMLDivElement>(null);

  const navGroups: NavGroup[] = useMemo(() => [
    {
      key: 'health',
      label: 'nav.group_health',
      icon: icons.heart,
      items: [
        { path: '/', label: 'nav.dashboard', icon: icons.dashboard },
        { path: '/search', label: 'common.search', icon: icons.search },
        { path: '/vitals', label: 'nav.vitals', icon: icons.heart },
        { path: '/labs', label: 'nav.labs', icon: icons.beaker },
        { path: '/medications', label: 'nav.medications', icon: icons.pill },
        { path: '/vaccinations', label: 'nav.vaccinations', icon: icons.syringe },
        { path: '/allergies', label: 'nav.allergies', icon: icons.alertTriangle },
        { path: '/diagnoses', label: 'nav.diagnoses', icon: icons.clipboardList },
      ],
    },
    {
      key: 'tracking',
      label: 'nav.group_tracking',
      icon: icons.activity,
      items: [
        { path: '/diary', label: 'nav.diary', icon: icons.bookOpen },
        { path: '/symptoms', label: 'nav.symptoms', icon: icons.activity },
        { path: '/appointments', label: 'nav.appointments', icon: icons.calendar },
        { path: '/tasks', label: 'nav.tasks', icon: icons.checkSquare },
      ],
    },
    {
      key: 'manage',
      label: 'nav.group_manage',
      icon: icons.fileText,
      items: [
        { path: '/documents', label: 'nav.documents', icon: icons.fileText },
        { path: '/contacts', label: 'nav.contacts', icon: icons.users },
        { path: '/family', label: 'nav.family', icon: icons.users },
        { path: '/shares', label: 'nav.shares', icon: icons.share },
        { path: '/emergency', label: 'nav.emergency', icon: icons.emergency },
      ],
    },
    {
      key: 'system',
      label: 'nav.group_system',
      icon: icons.settings,
      items: [
        { path: '/settings', label: 'nav.settings', icon: icons.settings },
        { path: '/calendar-feeds', label: 'nav.calendar_feeds', icon: icons.rss },
        { path: '/export', label: 'nav.export', icon: icons.download },
        { path: '/activity', label: 'nav.activity', icon: icons.activity },
        ...(role === 'admin' ? [{ path: '/admin', label: 'nav.admin', icon: icons.shield }] : []),
      ],
    },
  ], [role]);

  const isActive = (path: string) =>
    path === '/' ? location.pathname === '/' : location.pathname.startsWith(path);

  // Auto-derive active group from current route
  useEffect(() => {
    for (const group of navGroups) {
      if (group.items.some((item) => isActive(item.path))) {
        setActiveNavGroup(group.key);
        return;
      }
    }
  }, [location.pathname, navGroups, setActiveNavGroup]);

  // Close mobile menu on navigation
  useEffect(() => {
    setMobileMenuOpen(false);
  }, [location.pathname]);

  // Avatar menu click-outside
  useEffect(() => {
    function handleClickOutside(event: MouseEvent) {
      if (avatarMenuRef.current && !avatarMenuRef.current.contains(event.target as Node)) {
        setAvatarMenuOpen(false);
      }
    }
    if (avatarMenuOpen) {
      document.addEventListener('mousedown', handleClickOutside);
    }
    return () => document.removeEventListener('mousedown', handleClickOutside);
  }, [avatarMenuOpen]);

  // Avatar URL sync
  useEffect(() => {
    const onAvatarChanged = () => setAvatarUrl(localStorage.getItem('user_avatar'));
    window.addEventListener('avatar-changed', onAvatarChanged);
    return () => window.removeEventListener('avatar-changed', onAvatarChanged);
  }, []);

  const handleLogout = async () => {
    try { await api.post('/api/v1/auth/logout'); } catch { /* */ }
    clearAllKeys();
    logout();
    navigate('/login');
  };

  const displayName = email || 'User';
  const initials = email ? email.charAt(0).toUpperCase() : 'U';
  const activeGroupItems = navGroups.find((g) => g.key === activeNavGroup)?.items || [];

  const renderAvatar = () =>
    avatarUrl ? (
      <img src={avatarUrl} alt="" className="avatar-circle avatar-img" />
    ) : (
      <div className="avatar-circle">{initials}</div>
    );

  const renderAvatarDropdown = () =>
    avatarMenuOpen ? (
      <div className="avatar-dropdown">
        <div className="avatar-dropdown-header">
          <strong>{displayName}</strong>
          {role && <span className="user-role">{role}</span>}
        </div>
        <div className="avatar-dropdown-divider" />
        <Link to="/settings" className="avatar-dropdown-item" onClick={() => setAvatarMenuOpen(false)}>
          {t('nav.settings')}
        </Link>
        <button className="avatar-dropdown-item" onClick={toggleTheme}>
          {theme === 'light' ? t('nav.dark_mode') : t('nav.light_mode')}
        </button>
        <div className="avatar-dropdown-divider" />
        <button className="avatar-dropdown-item avatar-dropdown-danger" onClick={handleLogout}>
          {t('nav.logout')}
        </button>
      </div>
    ) : null;

  return (
    <div className={`app-layout${sidebarCollapsed ? ' sidebar-collapsed' : ''}`} data-theme={theme}>
      <SyncIndicator />

      {/* ═══════════════════════════════════════════════════
          DESKTOP SIDEBAR — hidden on tablet/mobile via CSS
         ═══════════════════════════════════════════════════ */}
      <aside className={`sidebar${sidebarCollapsed ? ' collapsed' : ''}`}>
        <div className="sidebar-header">
          <Link to="/" className="sidebar-brand">
            <svg className="brand-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" />
            </svg>
            {!sidebarCollapsed && <span className="brand-text">HealthVault</span>}
          </Link>
        </div>

        <nav className="sidebar-nav">
          {navGroups.map((group) => (
            <div key={group.key} className="nav-group">
              {!sidebarCollapsed && <div className="nav-group-label">{t(group.label)}</div>}
              {sidebarCollapsed && <div className="nav-divider" />}
              {group.items.map((item) => (
                <Link
                  key={item.path}
                  to={item.path}
                  className={`nav-item${isActive(item.path) ? ' active' : ''}`}
                >
                  <span className="nav-icon">{item.icon}</span>
                  {!sidebarCollapsed && <span className="nav-label">{t(item.label)}</span>}
                </Link>
              ))}
            </div>
          ))}
        </nav>

        <div className="sidebar-footer">
          <button className="btn-icon sidebar-theme-toggle" onClick={toggleTheme} aria-label="Toggle theme">
            {theme === 'light' ? icons.moon : icons.sun}
          </button>
          <button className="btn-icon sidebar-collapse-toggle" onClick={toggleSidebarCollapsed} aria-label="Toggle sidebar">
            {sidebarCollapsed ? icons.expand : icons.collapse}
          </button>
        </div>
      </aside>

      {/* ═══════════════════════════════════════════════════
          TABLET TOP-NAV — hidden on desktop/mobile via CSS
         ═══════════════════════════════════════════════════ */}
      <header className="tablet-nav">
        <div className="tablet-nav-primary">
          <Link to="/" className="tablet-brand">
            <svg className="brand-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" />
            </svg>
          </Link>
          <nav className="tablet-nav-groups">
            {navGroups.map((group) => (
              <button
                key={group.key}
                className={`tablet-group-tab${activeNavGroup === group.key ? ' active' : ''}`}
                onClick={() => setActiveNavGroup(group.key)}
              >
                {t(group.label)}
              </button>
            ))}
          </nav>
          <div className="tablet-nav-actions">
            <NotificationBell />
            <div className="avatar-menu" ref={avatarMenuRef}>
              <button className="avatar-btn" onClick={() => setAvatarMenuOpen((p) => !p)}>
                {renderAvatar()}
              </button>
              {renderAvatarDropdown()}
            </div>
          </div>
        </div>
        <nav className="tablet-subnav">
          {activeGroupItems.map((item) => (
            <Link
              key={item.path}
              to={item.path}
              className={`subnav-pill${isActive(item.path) ? ' active' : ''}`}
            >
              {t(item.label)}
            </Link>
          ))}
        </nav>
      </header>

      {/* ═══════════════════════════════════════════════════
          MOBILE HEADER — hidden on tablet/desktop via CSS
         ═══════════════════════════════════════════════════ */}
      <header className="mobile-header">
        <Link to="/" className="mobile-brand">
          <svg className="brand-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" />
          </svg>
          <span className="brand-text">HealthVault</span>
        </Link>
        <div className="mobile-header-actions">
          <NotificationBell />
          <button className="mobile-menu-btn" onClick={() => setMobileMenuOpen(true)} aria-label="Menu">
            {icons.menu}
          </button>
        </div>
      </header>

      {/* ═══════════════════════════════════════════════════
          MOBILE FULLSCREEN OVERLAY
         ═══════════════════════════════════════════════════ */}
      {mobileMenuOpen && (
        <>
          <div className="mobile-overlay-backdrop" onClick={() => setMobileMenuOpen(false)} />
          <div className="mobile-overlay">
            <div className="mobile-overlay-header">
              <span className="mobile-overlay-title">Menu</span>
              <button className="btn-icon" onClick={() => setMobileMenuOpen(false)} aria-label="Close">
                {icons.close}
              </button>
            </div>
            <nav className="mobile-overlay-nav">
              {navGroups.map((group) => (
                <div key={group.key} className="mobile-nav-group">
                  <div className="nav-group-label">{t(group.label)}</div>
                  {group.items.map((item) => (
                    <Link
                      key={item.path}
                      to={item.path}
                      className={`nav-item${isActive(item.path) ? ' active' : ''}`}
                    >
                      <span className="nav-icon">{item.icon}</span>
                      <span className="nav-label">{t(item.label)}</span>
                    </Link>
                  ))}
                </div>
              ))}
            </nav>
            <div className="mobile-overlay-footer">
              <button className="btn-ghost" onClick={toggleTheme}>
                {theme === 'light' ? icons.moon : icons.sun}
                <span>{theme === 'light' ? t('nav.dark_mode') : t('nav.light_mode')}</span>
              </button>
            </div>
          </div>
        </>
      )}

      {/* ═══════════════════════════════════════════════════
          MAIN CONTENT
         ═══════════════════════════════════════════════════ */}
      <main className="main-content">
        {/* Desktop-only topbar for notifications + avatar */}
        <header className="topbar">
          <div className="topbar-actions">
            <NotificationBell />
            <div className="avatar-menu" ref={!avatarMenuOpen ? undefined : avatarMenuRef}>
              <button className="avatar-btn" onClick={() => setAvatarMenuOpen((p) => !p)}>
                {renderAvatar()}
              </button>
              {renderAvatarDropdown()}
            </div>
          </div>
        </header>
        <Outlet />
      </main>
    </div>
  );
}
```

- [ ] **Step 2: Verify type-check**

Run: `cd web && npx tsc --noEmit`
Expected: No type errors

- [ ] **Step 3: Commit**

```bash
git add web/src/components/Layout.tsx
git commit -m "feat: rewrite Layout with adaptive navigation (desktop/tablet/mobile)"
```

---

### Task 5: Application Styles — Complete Rewrite

**Files:**
- Modify: `web/src/App.css` (complete rewrite, all 227+ class names preserved)

This is the largest task. Write the entire `web/src/App.css` file with all sections below combined into one file.

**CRITICAL:** All CSS class names used by page components must be preserved exactly. The new CSS redefines their styles using new design tokens but keeps the same selectors. No page component changes needed.

- [ ] **Step 1: Write the complete App.css**

The file must contain ALL of the following sections in order. Each section is delimited by a comment header. Write the complete file using the `Write` tool.

Refer to `docs/superpowers/specs/2026-03-31-redesign-design.md` for exact design token values, spacing, border-radius, and color decisions.

Key transformation rules applied to ALL sections:
- `border-radius: var(--radius-sm)` (6px) → `var(--radius)` (10px) for buttons/inputs
- `border-radius: var(--radius)` (8px) → `var(--radius-lg)` (14px) for cards
- `border-radius: var(--radius-lg)` → `var(--radius-xl)` (20px) for modals
- `padding` values use `--space-*` tokens where appropriate
- `transition` values use `--transition` / `--transition-fast` / `--transition-slow`
- Colors reference tokens (no hardcoded hex except in `rgba()` for semantic-light variants — use the token-defined `--*-light` variables instead)
- `font-family` uses `var(--font)` — no `-apple-system` or `BlinkMacSystemFont`
- Body font-size: `15px` (was `14px`)
- Heading hierarchy: h1=27px/700, h2=21px/600, h3=11px/600/uppercase
- Card shadow: `--shadow-sm` default, `--shadow-md` on hover
- Card border: `1px solid var(--border-subtle)` (was `--border`)
- Card border-radius: `var(--radius-lg)` (14px, was 8px)
- Button border-radius: `var(--radius)` (10px, was 6px)
- Input border-radius: `var(--radius)` (10px, was 6px)
- Input padding: `10px 14px` (was `8px 12px`)
- Badge border-radius: `var(--radius-sm)` (6px, was 20px pill)
- `.btn-primary` no longer `width: 100%` by default
- Focus ring: `0 0 0 3px var(--primary-light)` (same pattern, new color)

**Sections to include (preserving ALL existing class names):**

1. **Reset & Base** — `*, html, body, button/input/select/textarea, a, img/svg` — same structure as current sections 2-3
2. **Typography** — `h1-h6, .text-muted, p` — updated sizes per spec
3. **App Layout** — `.app-layout, .app-layout.sidebar-collapsed` — grid with new sidebar widths
4. **Desktop Sidebar** — `.sidebar, .sidebar.collapsed, .sidebar-header, .sidebar-brand, .brand-icon, .brand-text, .sidebar-nav, .nav-group, .nav-group-label, .nav-item, .nav-item.active, .nav-icon, .nav-label, .nav-divider, .sidebar-footer` — new active state (primary-light bg + primary text + 3px left border)
5. **Topbar & Avatar** — `.topbar, .topbar-actions, .avatar-menu, .avatar-btn, .avatar-circle, .avatar-img, .avatar-dropdown, .avatar-dropdown-header, .avatar-dropdown-divider, .avatar-dropdown-item, .avatar-dropdown-danger, .user-role` — desktop-only topbar
6. **Tablet Navigation** — `.tablet-nav, .tablet-nav-primary, .tablet-brand, .tablet-nav-groups, .tablet-group-tab, .tablet-nav-actions, .tablet-subnav, .subnav-pill` — NEW section
7. **Mobile Navigation** — `.mobile-header, .mobile-brand, .mobile-header-actions, .mobile-menu-btn, .mobile-overlay-backdrop, .mobile-overlay, .mobile-overlay-header, .mobile-overlay-nav, .mobile-nav-group, .mobile-overlay-footer` — NEW section
8. **Main Content & Page** — `.main-content, .page, .page h2, .page-header, .page-actions` — updated padding
9. **Cards** — `.card, .card h3, .card-header, .card-link, .card--status-*` — new radius/shadow/border
10. **Dashboard** — `.dashboard-grid, .stats-row, .stat-card, .stat-value, .stat-label, .dash-list, .dash-item, .dash-meta` — updated tokens
11. **Buttons** — `.btn, .btn-primary, .btn-secondary, .btn-ghost, .btn-danger, .btn-add, .btn-icon, .btn-text, .btn-sm, .btn-icon-sm` — new radius/padding
12. **Forms** — `.form-group, .form-group label, .form-group input/select/textarea, .form-group--float, .form-row, .form-actions, .vital-form, .profile-selector, .metric-selector, .radio-group, .toggle-label` — new radius/padding/focus
13. **Alerts** — `.alert, .alert-error, .alert-warning, .alert-success, .alert-info` — new semantic colors
14. **Page Header & Auth** — `.auth-page, .auth-card, .auth-tagline, .onboarding-page, .onboarding-card, .progress-bar, .progress-bar-fill, .step-indicator, .recovery-grid, .recovery-code` — updated tokens
15. **Tables** — `.table-scroll, .data-table, .data-table th, .data-table td, .data-table tbody tr` — updated tokens
16. **Badges** — `.badge, .badge-active, .badge-inactive, .badge-scheduled, .badge-completed, .badge-cancelled, .badge-missed, .badge-danger, .badge-info, .badge-success, .badge-warning, .tag, .status-normal, .status-borderline, .status-abnormal, .status-critical` — new radius (6px, was 20px pill)
17. **List Items** — `.med-list, .appt-list, .doc-list, .task-list, .med-item, .appt-item, .doc-item, .task-item, .med-info, .appt-info, .doc-info, .task-info, .med-name, .med-details, .med-meta, .med-actions, .appt-actions` — updated tokens
18. **Appointments** — `.appt-date, .appt-day, .appt-month, .appt-time, .appt-title, .appt-type, .appt-location, .appt-past` — updated colors
19. **Documents** — `.doc-icon, .doc-name, .doc-meta, .doc-tags, .doc-category-badge, .doc-upload-zone, .doc-upload-text, .doc-upload-controls, .doc-upload-progress, .doc-upload-spinner, .doc-preview-*, .doc-edit-form, .doc-linked-*, .drop-active, .drop-overlay, .drop-message` — updated tokens
20. **Timeline** — `.timeline, .timeline-item, .timeline-icon, .timeline-content, .timeline-header, .timeline-title, .timeline-date, .timeline-meta, .timeline-desc, .timeline-location` — updated tokens
21. **Charts** — `.chart-card, .chart-header, .chart-empty, .chart-filters, .chart-range-btn, .vital-chart-tabs, .vital-chart-tab, .vital-latest, .vital-latest-item, .vital-latest-value, .vital-latest-unit, .vital-latest-label, .vital-latest-date, .metric-toggles, .metric-toggle, .metric-dot` — updated tokens
22. **Tasks** — `.task-overdue, .task-check, .task-check.task-done, .task-title, .task-completed-text, .task-meta` — updated tokens
23. **Contacts** — `.contact-grid, .contact-card, .contact-header, .contact-avatar, .contact-name, .contact-detail` — updated tokens
24. **Symptoms** — `.symptom-entries, .symptom-entry` — updated tokens
25. **Quick Add** — `.quick-add-form, .quick-input, .quick-label, .quick-separator` — updated tokens
26. **Labs** — `.lab-list, .lab-item, .lab-header, .lab-values, .expand-icon` — updated tokens
27. **Settings** — `.settings-section, .setting-row, .storage-bar, .storage-fill, .avatar-upload-*, .crop-*, .security-*, .totp-*, .session-*, .toggle-switch, .toggle-switch-label, .toggle-switch-desc, .settings-form-row, .settings-unit` — updated tokens
28. **Search** — `.search-box, .search-input, .search-results, .search-items, .search-item, .search-item-name` — updated tokens
29. **Notifications** — `.notif-container, .notif-bell, .notif-badge, .notif-dropdown, .notif-header, .notif-title, .notif-mark-all, .notif-empty, .notif-list, .notif-item, .notif-unread, .notif-icon, .notif-content, .notif-item-title, .notif-body, .notif-time, .notif-dot` — updated tokens
30. **Sync Indicator** — `.sync-indicator, .sync-dot, .sync-offline, .sync-syncing, .sync-text` — updated tokens
31. **Error Boundary** — `.error-boundary, .error-card` — updated tokens
32. **Trend Panel** — `.trend-panel, .trend-grid, .trend-stat, .trend-label, .trend-value` — updated tokens
33. **Calendar Feeds** — `.feed-url-box, .feed-url` — updated tokens
34. **Modals** — `.modal-overlay, .modal, .modal-header, .modal-close, .modal-body, .modal-footer, .confirm-modal, .confirm-icon, .confirm-title, .confirm-message, .confirm-actions` — new radius (20px), scale animation
35. **Stepper** — `.stepper, .stepper-steps, .stepper-track, .stepper-step, .stepper-dot, .stepper-label, .stepper-line, .stepper-line-fill, .stepper-text, .stepper-connector` — updated tokens
36. **Vital Picker** — `.vital-picker, .vital-chip, .vital-chip-icon, .vital-chip-label, .vital-fieldset` — updated tokens
37. **Empty State** — `.empty-state, .empty-state-icon, .empty-state-title, .empty-state-subtitle` — updated tokens
38. **Toast** — `.toast-container, .toast, .toast-success, .toast-error, .toast-warning, .toast-info, .toast-icon, .toast-message, .toast-close` — updated tokens
39. **Loading** — `.skeleton, .spinner` — updated tokens
40. **Export** — `.export-grid, .export-card, .export-card-icon, .export-card-body, .export-card-title, .export-card-desc, .export-card-btn, .export-spinner` — updated tokens
41. **Admin** — `.admin-tabs, .admin-tab, .admin-user-list, .admin-user-card, .admin-user-disabled, .admin-user-header, .admin-user-avatar, .admin-user-info, .admin-user-name, .admin-user-email, .admin-user-actions` — updated tokens
42. **Share View** — `.share-page, .share-container, .share-header, .share-brand, .share-tabs, .share-tab, .share-tab-label, .share-tab-count, .share-content, .share-table, .share-vital-filters, .share-vital-filter, .share-footer` — updated tokens
43. **Toggle Switches** — `.toggle-switch input[type="checkbox"]` — updated colors
44. **View Tabs & Toolbar** — `.view-toolbar, .view-toolbar-right, .view-tabs, .view-tab` — updated tokens
45. **OCR Upload** — `.ocr-upload, .ocr-dropzone, .ocr-progress, .ocr-spinner` — updated tokens
46. **Utility** — `.visually-hidden, .truncate` — unchanged
47. **Focus & Accessibility** — `:focus-visible, scrollbar styles, ::selection` — updated primary color
48. **Animations & Keyframes** — `pageIn, cardIn, dropdownIn, overlayIn, modalIn, slideUp, pulse, spin, shimmer, doc-spin, toastIn, toastOut` — updated timing to use `var(--duration-*)` where possible
49. **Print** — `@media print` — updated for new nav structure (hide `.sidebar, .tablet-nav, .mobile-header`)
50. **Responsive: Tablet (640–1023px)** — `@media (min-width: 640px) and (max-width: 1023px)` — show tablet-nav, hide sidebar/mobile-header, adapt grids to 6-col
51. **Responsive: Mobile (<640px)** — Mobile is the BASE (mobile-first). Desktop/tablet overrides added via min-width queries. Base styles: hide sidebar + tablet-nav, show mobile-header, single-column grids, bottom-sheet modals
52. **Responsive: Desktop (≥1024px)** — `@media (min-width: 1024px)` — show sidebar, hide tablet-nav/mobile-header, grid layout with sidebar
53. **Reduced Motion** — `@media (prefers-reduced-motion: reduce)` — disable all animations

**IMPORTANT mobile-first structure:**

The CSS should be written mobile-first:
1. Base styles = mobile layout (no sidebar, single column, mobile-header visible)
2. `@media (min-width: 640px)` = tablet overrides (show tablet-nav, 6-col grid)
3. `@media (min-width: 1024px)` = desktop overrides (show sidebar, 12-col grid)

Visibility rules:
```css
/* Base: mobile */
.sidebar { display: none; }
.tablet-nav { display: none; }
.topbar { display: none; }
.mobile-header { display: flex; }

/* Tablet */
@media (min-width: 640px) {
  .mobile-header { display: none; }
  .tablet-nav { display: block; }
}

/* Desktop */
@media (min-width: 1024px) {
  .tablet-nav { display: none; }
  .sidebar { display: flex; }
  .topbar { display: flex; }
  .app-layout { grid-template-columns: var(--sidebar-w) 1fr; }
  .app-layout.sidebar-collapsed { grid-template-columns: var(--sidebar-collapsed) 1fr; }
}
```

- [ ] **Step 2: Verify build**

Run: `cd web && npx tsc --noEmit && npx vite build --mode development 2>&1 | tail -5`
Expected: No errors, build succeeds

- [ ] **Step 3: Commit**

```bash
git add web/src/App.css
git commit -m "style: complete CSS rewrite with warm design system and adaptive navigation"
```

---

### Task 6: Verification & QA

**Files:** None (verification only)

- [ ] **Step 1: Full build**

Run: `cd web && npx vite build`
Expected: Build succeeds with no errors

- [ ] **Step 2: Type check**

Run: `cd web && npx tsc --noEmit`
Expected: No type errors

- [ ] **Step 3: Run existing tests**

Run: `cd web && npx vitest run --reporter=verbose 2>&1 | tail -20`
Expected: All existing tests pass (no component logic changed)

- [ ] **Step 4: Visual QA checklist**

Start dev server and verify in browser:

```bash
cd /home/pr0ph37/Entwicklung/healthvault && make dev
```

Check:
- [ ] Desktop (≥1024px): Sidebar visible with 4 groups, collapsible, terracotta active state
- [ ] Tablet (640–1023px): Top-bar with group tabs, scrollable sub-nav pills
- [ ] Mobile (<640px): Slim header + hamburger opens fullscreen overlay
- [ ] Dark mode: warm brown tones, readable text, no cold grays
- [ ] Cards: 14px radius, soft shadow, hover elevation
- [ ] Buttons: 10px radius, terracotta primary
- [ ] Forms: 10px radius, terracotta focus ring
- [ ] Tables: data visible, proper spacing
- [ ] Modals: centered on desktop, bottom-sheet on mobile
- [ ] All 28 pages render without broken layouts

- [ ] **Step 5: Final commit (if QA fixes needed)**

```bash
git add -A
git commit -m "fix: visual QA adjustments for redesign"
```
